<script>
    $(document).ready(function () {
        $("#locale").val("<%= locale %>");
    });
</script>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
    &#9776;
  </button>

  <div class="collapse navbar-collapse" id="navbarToggler">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><%= t(:about) %></a>
        <div class="dropdown-menu" style="text-align: left; padding-left: 6px">
          <%= link_to(t(:one_million_voices), about_index_path, :class => "nav-link") %>
          <%= link_to(t(:who_we_are), who_we_are_index_path, :class => "nav-link") %>
          <%= link_to(t(:manual), manual_index_path, :class => "nav-link") %>
        </div>
      </li>

      <li class="nav-item">
        <%= link_to(t(:map), root_path, :class => "nav-link") %>
      </li>

      <li class="nav-item">
        <%= link_to(t(:locations), locais_path, :class => "nav-link") %>
      </li>

      <li class="nav-item">
        <%= link_to(t(:practices), practices_path, :class => "nav-link") %>
      </li>

    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li class="nav-item">
        <%= link_to(t(:accounts), usuarios_path, :class => "nav-link") %>
      </li>
      <% if current_usuario %>
        <li class="nav-item">
          <%= link_to("#{t(:logout)}", destroy_usuario_session_path, :method => :delete, :class => "nav-link") %>
        </li>

        <li class="nav-item">
          <%= link_to "(#{current_usuario.email})", usuario_path(current_usuario), :class => "nav-link text-danger" %>
        </li>
      <% else %>
        <li class="nav-item">
          <strong><%= link_to(t(:login), new_usuario_session_path, :class => "nav-link") %></strong>
        </li>
        <li>
          <strong><%= link_to t(:sign_up), new_usuario_registration_path, :class => "nav-link text-danger" %></strong>
        </li>
      <% end %>

      <li>
        <select id="locale" onchange="if (this.value) window.location.href='<%= request.path %>?locale=' + this.value" class="form-control">
          <option value="pt-BR">Português</option>
          <option value="es">Español</option>
          <option value="en">English</option>
          <option value="fr">Français</option>
        </select>
      </li>
    </ul>
  </div>
</nav>

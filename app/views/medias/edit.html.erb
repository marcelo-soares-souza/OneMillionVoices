<% title "Editing #{@media.description}" %>

<br><br><br><br>

<ul class="nav nav-tabs flex-column flex-sm-row">
  <li class="nav-item">
    <% if params[:practice_id] %>
      <%= link_to t(:practices), practices_path, :class => "nav-link" %>
    <% end %>
  </li>

  <li class="nav-item">
    <% if params[:practice_id] %>
      <%= link_to @practice.name, practice_path(@practice), :class => "nav-link" %>
    <% end %>
  </li>

  <li class="nav-item">
    <% if params[:practice_id] %>
      <%= link_to "Fotos (#{@practice.medias.count})", practice_medias_path, :class => "nav-link" %>
    <% end %>
  </li>

  <li class="nav-item">
    <% if params[:practice_id] %>
      <%= link_to "#{t(:edit)} #{@media.description}", edit_practice_media_path, :class => "nav-link active" %>
    <% end %>
  </li>
</ul>

<br>

<%= render 'form', media: @media %>

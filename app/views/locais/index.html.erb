<%= render 'layouts/dataTable' %>

<% title t(:locations) %>

<%= link_to t("location.register_new_location"), new_local_path, :class => "btn btn-primary", :style => "float:right;" %>

<br><br><br>

<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to t(:locations) + " #{ @usuario.blank? ? '' : '(' + @usuario.nome.truncate(16) + ')' }", "#", :class => "nav-link active" %>
  </li>
</ul>

<% if @locais.empty? %>
  <br>
  <h2><%= t(:no_data_has_been_registered_so_far) %></h2>
<% else %>
  <br>

  <div class="row">
    <% @locais.each do |local| %>

      <div class="col-sm-6">
        <div class="card">
          <br>
          <div  class="card-img-top thumbnail-mini"><%= link_to image_tag(local.imagem.url(:medium)), local_path(local), alt: local.nome, title: local.nome %></div>
          <div class="card-body">
            <h5 class="card-title"><%= link_to local.nome, local_path(local) %></h5>
            <p class="card-text" style="text-align: center;"><%= @tipos.key(local.tipo) %></p>
            <% if usuario_signed_in? %>
              <% if current_usuario.admin? or local.usuario.id == current_usuario.id %>
                <td><%= link_to t(:edit), edit_local_path(local), :class => 'btn btn-sm btn-success' %></td>
                <td><%= link_to t(:destroy), local_path(local), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-sm btn-danger' %></td>
              <% else %>
                <br><br>
              <% end %>
            <% end %>

            <!-- <a href="#" class="btn btn-primary">Go somewhere</a> -->
          </div>
        </div><br>
      </div>
    <% end %>
  </div>

  <% if not params[:usuario_id] %>
    <div>
      <%= paginate @locais %>
    </div>
  <% end %>

  <script> $(document).ready(function () { dataTables(); }); </script>
<% end %>
